import type { PaperColor } from '../../shared/schema';

export const PAPER_COLORS: readonly PaperColor[] = ['cream', 'rose', 'sky'];

export const PAPER_LABELS: Record<PaperColor, string> = {
  cream: 'クリーム',
  rose: 'もも',
  sky: 'そら',
};
